import { Router } from 'express';

const router = Router();

// Placeholder notification routes for FlowPilot.
// Additional endpoints (e.g., listing notifications, classifying interrupts)
// can be added here later.

export default router;

